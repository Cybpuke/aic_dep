Create DB folder and permissions
sudo mkdir -p /mnt/data/postgres
sudo chown -R 999:999 /mnt/data/postgres

Edit docker-compose.yml for db volume

Change your db service to:

services:
  db:
    volumes:
      - /mnt/data/postgres:/var/lib/postgresql/data
